[package]
name = "frey"
version = "0.4.0"
edition = "2024"

[dependencies]
inkwell = { version = "0.9.0", default-features = false, features = ["llvm22-1", "target-x86"] }
llvm-sys = { version = "221.0.1", features = ["prefer-dynamic"] }

[target.'cfg(target_os = "windows")'.dependencies]
cc = "1"
