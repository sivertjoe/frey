const square = (x: Int) -> Int {
    x * x
}

const add = (a: Int, b: Int) -> Int {
    a + b
}

const main = () -> Int {
    let x: Int = 5
    let z = 5 as Float;
    let y = x |> square |> add(10)

    let count = 0
    count += 1;

    print(y)

    0
}
